import 'package:flutter/material.dart';

//Renomear depois - corpo do home page

class HomePageBody extends StatelessWidget {
  const HomePageBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Home Page')
      ],
    );
  }
}