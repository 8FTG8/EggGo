import 'package:flutter/material.dart';

class NotaFiscalPage extends StatelessWidget {
  const NotaFiscalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Nota Fiscal Page')
      ],
    );
  }
}