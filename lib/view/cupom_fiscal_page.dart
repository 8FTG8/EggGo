import 'package:flutter/material.dart';

class CupomFiscalPage extends StatelessWidget {
  const CupomFiscalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Cupom Fiscal')
      ],
    );
  }
}